document.querySelectorAll('.draggable').forEach(card => { card.addEventListener('dragstart', (e) => { e.dataTransfer.setData('text/plain', e.target.id); }); card.addEventListener('dragover', (e) => { e.preventDefault(); }); card.addEventListener('drop', (e) => { e.preventDefault(); const id = e.dataTransfer.getData('text'); const draggableElement = document.getElementById(id); const dropzone = e.target.closest('.draggable'); if (dropzone && dropzone !== draggableElement) { const temp = document.createElement('div'); dropzone.parentNode.insertBefore(temp, dropzone); draggableElement.parentNode.insertBefore(dropzone, draggableElement); temp.parentNode.insertBefore(draggableElement, temp); temp.parentNode.removeChild(temp); } }); });